# Query Processing Design

## Overview

Query processing transforms user search queries into a format compatible with the document index.

The query follows the same preprocessing pipeline used during document processing.

---

## Pipeline

User Query
